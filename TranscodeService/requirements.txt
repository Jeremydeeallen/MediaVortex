# TranscodeService Dependencies
# Core dependencies for the transcoding microservice

# System monitoring
psutil>=5.9.0

# Process naming
setproctitle

# Note: All other functionality uses built-in Python modules
# The main dependencies (ProcessTranscodeQueueService, DatabaseManager, etc.)
# are imported from the parent MediaVortex project
