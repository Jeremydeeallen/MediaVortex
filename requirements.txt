# Core dependencies
flask>=2.3.0
psutil>=5.9.0
setproctitle
pythonnet>=3.0.0

# Database
psycopg2-binary>=2.9.0

# Configuration and logging
configparser

# Web interface
flask-cors
requests

# Video transcoding (FFmpeg integration)
ffmpeg-python>=0.2.0
scenedetect>=0.6.0

# Speech-language detection (post-Whisper transcript -> language code)
langdetect>=1.0.9

# SSH connectivity (Jellyfin server)
paramiko>=3.0.0

# Development tools
pytest
hypothesis>=6.0.0
black
flake8

# Note: pathlib, shutil, os, tempfile, subprocess, logging, json are built-in Python modules
