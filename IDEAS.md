# Ideas

- 2026-06-06 | Multiple audio languages: keep current behavior as default track; add 2nd audio track with aggressive dialogue enhancement (night mode / hearing-impaired compression) for older ears
- 2026-06-09 | Push compliance Gates into a SQL view (`v_MediaFilesGateStatus` = `MediaFiles CROSS JOIN ComplianceGates` + first-fail CASE WHEN). Collapses the 8 `IComplianceGate` impls + chain + interface from the compliance-solid-refactor directive into one view + ~30 LOC Python reader; `RecomputeForFiles` becomes a single SQL UPDATE for the gate column. Considered for the original directive, deferred to a follow-up after the engine shipped.
- 2026-06-12 | Per-share-root audio policy. Add a `StorageRootAudioPolicy` table (one row per StorageRootId) carrying flags `AllowMissingAudio` (bool, default FALSE) + `LoudnessTargetMode` (enum: 'Linear', 'Refuse', 'BypassWithMeasurement'). Replaces today's library-wide policy from `linear-loudnorm.feature.md` ("linear or refused -- never quietly different") and `audio-completion.feature.md` AudioCorruptSuspect blocks with per-share-root rules. Operator concern: the `xxx` share (adult content; 17 rows already manually flagged `AudioDamageNotMaterial=TRUE` 2026-06-08) should tolerate missing audio (not a hold-out reason on that share) AND still always run loudness normalization on the audio it does carry. Today both behaviors are library-wide hard rules. Feature touches: `Features/AudioCompletion/`, `Features/LoudnessAnalysis/`, `Features/TranscodeQueue/QueueManagementBusinessService` (compliance cascade), `Features/Compliance/Gates/AudioStreamGate.py` + `AudioCorruptSuspectGate.py` (need StorageRootId-aware variants). Out of scope for any current directive; size estimate: ~2-3 day directive.
- 2026-06-19 | Bitrate-aware Transcode gating to prevent transcode-of-already-efficient sources. Current `EstimatedSavingsMB` compares source size to projected output size (using profile `TargetVideoKbps + TargetAudioKbps * duration`), so a 332 MB 720p HEVC source (already ~2 Mbps, BPP 0.09) gets routed to Transcode when the canary 720p AV1 target is ~450 kbps -- savings clear the 150 MB threshold but the result is generation-loss (~80-85 VMAF vs the canary's validated 92.92 baseline on uncompressed sources). Three approach options: (a) `MinSourceVideoBitrateRatio` on `transcoderules` (global, tiny effort, OK short-term); (b) `MinSourceBpp` on `profiles` (per-profile, normalizes across resolutions, default ~0.04 bpp on canary tiers, scales as profile catalog grows -- preferred); (c) codec-bucketed thresholds (`MinRatioForH264` vs `MinRatioForEfficientCodecs`, overkill until empirical data exists). Preferred: ship `MinSourceBpp` on `profiles` next to existing `audiobitratekbps`, plumb through `EffectiveProfile`, branch in `TranscodeOperation.Apply` before the savings-MB rule, add Settings input. Validate default via canary VMAF run on 30 Rock S01E01 (BPP 0.09 source) first to confirm the 0.04 threshold lands files in the right buckets.
- 2026-06-19 | Compliance refactor: collapse 4-operation `IComplianceOperation` hierarchy + `OperationResult` + `OperationsNeeded` set + `WorkBucket` derivation into three independent pure checks (`VideoCheck`, `ContainerCheck`, `AudioCheck`) + one bucket lookup table. Decision becomes `bucket = LOOKUP[(video_ok, container_ok, audio_ok)]` where the table is `{(T,T,T): None, (T,T,F): 'AudioFix', (T,F,T): 'Remux', (T,F,F): 'Remux', (F,*,*): 'Transcode'}`. Eliminates the 4 Rules tables (replaced with `VideoRules`/`ContainerRules`/`AudioRules`), the `chk_compliance_consistency` constraint, and the `OperationsNeededCsv` column. **Invariant the refactor establishes:** nothing downstream of compliance reads anything but `WorkBucket` -- workers, dashboards, queue routing all consume bucket only. Adding/removing/retuning a criterion then never ripples beyond one check function + one rule column + one Settings input. Surfaces the 30 Rock S01E01 routing bug for free (BPP-aware video check returns True for already-efficient HEVC, file falls to Remux, audio gets fixed, video stream-copies). Out of scope for any current audio-vertical work; size estimate: ~1-2 day directive. Sequenced AFTER audio vertical closes to 100% (Phase 2 in 2026-06-19 sequencing convo).
- 2026-07-03 | Rename TranscodeJob umbrella name to MediaJob. Current umbrella "Transcode" collides with the media-industry strict definition (re-encode only). MediaVortex's TranscodeJob covers remux + audio-fix + container-fix too, which forces GLOSSARY.md to carry two senses of the word "transcode" (job type vs bucket) and makes docs harder for new readers. Rename target: MediaJob (vague enough to cover any FFmpeg-driven media transformation). After rename, Plan values map cleanly to industry terms: VideoOp={Transcode, Copy}, AudioOp={Transcode, Copy}, ContainerOp={Swap, Keep}; job classifications (remux/transcode/audio-fix) become derived from plan. Scope: DB tables (TranscodeQueue -> MediaJobQueue, TranscodeAttempts -> MediaJobAttempts, Workers.TranscodeEnabled -> MediaJobEnabled or MediaCapable, ActiveJobs), Python packages (Features/TranscodeJob/ -> Features/MediaJob/, Features/TranscodeQueue/ -> Features/MediaJobQueue/), claim helpers (ClaimNextPendingTranscodeJob -> ClaimNextPendingMediaJob), flow docs (transcode.flow.md -> media-job.flow.md), every Transcode*.feature.md, operator surfaces (/Work/Transcode route + page labels + log messages + error strings), code anchors (# see transcode.STN -> # see media-job.STN in every anchored file). Also fix the transcodeattempts.ffpmpegcommand typo in the same migration (rename to mediajobattempts.ffmpegcommand). Estimated blast radius: ~200-300 files touched. Own directive slug: media-job-rename. Sequence: run BEFORE any big logical directive that touches transcode surfaces so files are edited once with the new name, not twice. Not doing now -- deferred until after transcode-flow-canonical closes with current names. The transcode-flow-canonical directive is intentionally accepting the two-sense ambiguity (documented in GLOSSARY.md) as a transition cost until this rename lands.
- 2026-07-20 | **IMPORTANT** audio-language-detection for the ~6,664 files with und/empty AudioLanguages. Sonarr/Radarr rely on file-level audio language tags for release-selection and upgrade policy; when the tag is und or empty they cannot decide, and MediaVortex's SelectPreferredAudioStream falls back to Track 0 blindly (same bug class as the 88 files just fixed via delete+redownload, but for the un-transcoded backlog). Operator has confirmed foreign languages are fine (son watches anime); only und + empty are unacceptable. Scope: new vertical Features/AudioLanguageDetection/, model speechbrain/lang-id-voxlingua107-ecapa (~1s per file, 107 languages, runs on wakko XPU or dot NVIDIA - both already have torch), 30-60s middle-of-file audio sample. Writeback path: metadata-only ffmpeg mux (`-c copy -metadata:s:a:X language=<detected>` — no re-encode, ~1-2s/file) so the tag lands in the container Sonarr reads. Plus DB writeback to MediaFiles.AudioLanguages + HasExplicitEnglishAudio. Pipeline hook on transcode entry (detect first if tag missing, use for output metadata). Background sweeper same shape as AudioRemeasurementRunner drains the 6,664 backlog at low priority. Idempotent. Est. throughput ~3s/file, parallel across wakko+dot → ~3-4 hours to drain full backlog. requirements.txt += speechbrain (~50MB, no new deploy path). Deferred because: 6k-file backlog is scope-heavy; current focus is e2e-bug-fixes and the transcode-reliability fixes that unblock daily transcoding. Do not start until operator explicitly resumes. **Effect of the gap while deferred:** any file with und/empty AudioLanguages that enters the transcode pipeline will have Dialog Boost content built from source Track 0 blindly (still labeled 'Dialog Boost (eng)'). Same content-language-mismatch bug we just paid to delete 87 files for on Sonarr. Every new transcode of a und-tagged source rebuilds the problem. The 5f59d91 fix (PreEncodeAudioPipeline._SelectPreferredAudioIndex → English preferred when tagged) does NOT help when NO stream carries the eng tag — the fallback returns the first stream, which reintroduces the bug. **Operator directive:** after e2e-bug-fixes closes, remind operator that this flaw exists and continues to accumulate degraded Dialog Boost content on every new transcode of an und/empty-tagged source.
- 2026-07-23 | Restatement of MediaVortex domain in one page — the SoT to point at when scope threatens to expand. Domain (minimal): scan library; per file, one decision function with five branches — IF audio-only container -> out of scope; IF SizeMB/DurationMin < THRESHOLD -> skip; IF video codec not in allowlist -> transcode; IF container not in allowlist -> remux; IF audio needs normalization -> audio-fix; ELSE skip. Four operators (Skip/Remux/AudioFix/Transcode). Non-destructive archive (MediaFilesArchive). Jellyfin notify on change. Candidates for deletion, not further directives: 3 compliance verticals + 3 rules tables; AdequacyGate as separate service (redundant with size/duration ratio); WorkBucket column (derived data promoted to storage); Profile tier ladders (Family x Tier x ContentClass matrix — real domain: one profile per resolution); VMAF confidence stats + smart sampling; PostTranscodeGateConfig disposition matrix; Plan tuples with 4 ops; 5 _old_2026_06_21 archive tables never dropped; 12+ compliance directives in 6 weeks; 271KB directive doc, 34 Resets on the active directive. Rule for future changes: elegance is not the domain. Domain is shrink files that are big enough to matter, preserve everything else, do not destroy sources. Any proposed change that does not map to one of the four operators or the five-branch decision = candidate for refusal, not a new directive. Prevention: if a rewrite is being pitched, first prove the current shape ACTIVELY causes bugs (not invites bugs, not structurally suboptimal). Guardrails + one narrow fix beat structural refactor unless production is on fire.
