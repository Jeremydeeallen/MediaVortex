# MicroServiceQualityTest Dependencies
# Core dependencies for quality testing microservice

# Core dependencies
flask>=2.3.0
psutil>=5.9.0
setproctitle

# Configuration and logging
configparser

# Video transcoding (FFmpeg integration)
ffmpeg-python>=0.2.0

# Note: pathlib, shutil, os, tempfile, subprocess, logging, json, signal, threading are built-in Python modules
