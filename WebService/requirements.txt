# WebService Dependencies
# Core dependencies for the web interface

# Web framework
flask>=2.3.0
flask-cors

# Production WSGI server (replaces Flask dev server -- handles concurrent requests cleanly)
waitress>=2.1.0

# HTTP requests
requests

# System monitoring
psutil>=5.9.0

# Process naming
setproctitle

# Note: pathlib, shutil, os, tempfile, subprocess, logging, json, configparser are built-in Python modules

# Database
psycopg2-binary>=2.9.0

WinTmp